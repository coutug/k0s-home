apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: dns
  namespace: flux-system
spec:
  interval: 20m
  dependsOn:
    - name: network
  sourceRef:
    kind: GitRepository
    name: k0s-home
  decryption:
    provider: sops
    secretRef:
      name: sops-age
  path: components/dns
  prune: true
  timeout: 10m

apiVersion: v1
kind: Service
metadata:
  name: jellyfin
  labels:
    exposure: tailnet
  annotations:
    lbipam.cilium.io/ips: 10.250.0.12
spec:
  type: LoadBalancer
  selector:
    app: jellyfin
  ports:
    - name: web
      port: 8096
      targetPort: web
      protocol: TCP

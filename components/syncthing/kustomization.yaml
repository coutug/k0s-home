apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: syncthing

resources:
  - ns.yaml
  - pvc.yaml
  - deployment.yaml
  - service.yaml

secretGenerator:
  - name: syncthing-configd
    files:
      - ./configd.conf